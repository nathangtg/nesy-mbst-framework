# LaTeX auxiliary — all reproducible via `make` or pdflatex
*.aux
*.bbl
*.blg
*.log
*.out
*.toc
*.lof
*.lot
*.fls
*.fdb_latexmk
*.synctex.gz
*.nav
*.snm
*.vrb

# Keep the compiled PDF committed as the canonical artefact
# (uncomment the line below to exclude it)
# main.pdf
